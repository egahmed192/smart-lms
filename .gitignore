# Ignore all .cache files
*.cache

# Moodle data and local cache directories (never commit runtime data)
/moodledata/

# Local Moodle configuration (contains environment-specific secrets/paths)
/moodle/config.php

